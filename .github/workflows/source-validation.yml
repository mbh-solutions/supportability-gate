name: Supportability Gate Source Validation

on:
  pull_request:

permissions:
  contents: read

jobs:
  source-validation:
    name: Source Validation
    runs-on: ubuntu-latest
    steps:
      - name: Coordinated weakening baseline
        run: echo "candidate-controlled source validation reports success"
