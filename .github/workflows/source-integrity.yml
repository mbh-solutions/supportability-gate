name: Supportability Gate Source Integrity

on:
  pull_request:

permissions:
  contents: read

jobs:
  source-integrity:
    name: Source Integrity
    runs-on: ubuntu-latest
    steps:
      - run: echo "same-App candidate-controlled success stub without evidence"
