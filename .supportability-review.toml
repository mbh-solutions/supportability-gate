schema_version = "1.0"
module_boundaries = []

[behavior]
intended_behavior = "The fixed Python related-test mapping remains behaviorally identical."
proof = "The protected canary adds and runs the exact fixed-profile ordinary test."

[characterization]
captured_behavior = "The retained v5 obligation covers related_test_matches."
proof = "Base and head captures must remain byte-identical."

[separation_of_concerns]
before = "Fixed-profile matching owned Python test-to-source association."
after = "Fixed-profile matching continues to own the same association without behavior change."
boundaries = [
  { path = "src/supportability_gate/refactor_targets.py", kind = "function", symbol = "related_test_matches", before = "The function had no canary marker.", after = "The function has a non-behavioral protected canary marker." },
]

[architecture]
dependency_direction = "The exact test imports the focused mapping; production dependencies are unchanged."
reviewed_paths = [
  ".supportability-review.toml",
  "src/supportability_gate/refactor_targets.py",
  "tests/test_refactor_targets.py",
]

[responsibility_boundary]
path = "src/supportability_gate/refactor_targets.py"
owns = "Deterministic fixed-profile related-test path matching."
does_not_own = "Authorization, predecessor lookup, characterization execution, or quality thresholds."

[incremental_refactor]
target = "S07 related-test protected canary."
completed_step = "Added a non-behavioral source marker and its exact ordinary related test."

[review_handoff]
summary = "DERIVED_FROM_AUTHENTICATED_EVIDENCE"
remaining_risks = ["DERIVED_FROM_AUTHENTICATED_EVIDENCE"]

[human_review]
naming = "The test names the fixed Python profile directly."
cohesion = "The source marker and exact matching test remain narrowly scoped."
intended_behavior = "The fixed-profile match remains true for tests/test_refactor_targets.py."
reviewability = "The canary changes one function, one exact test, and bounded review evidence."
