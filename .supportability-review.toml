schema_version = "1.0"

[behavior]
intended_behavior = "The protected Gate evaluates one repository containing Python and TypeScript production source."
proof = "tests/mixed_canary.test.ts and the repository's existing Python proof run through the fixed mixed profile."

[characterization]
captured_behavior = "The exact base and head captures run one stable TypeScript scenario command."
proof = "tests/characterization/s13-mixed-stack-canary.characterization.mjs"

[separation_of_concerns]
before = "The protected canary repository exercised only Python production source."
after = "One temporary TypeScript function proves both fixed profiles execute in one protected evaluation."

[[separation_of_concerns.boundaries]]
path = "src/mixed_canary.ts"
kind = "function"
symbol = "mixedStackLabel"
before = "No TypeScript production responsibility existed in the canary."
after = "The temporary function owns one TypeScript runtime label."

[architecture]
dependency_direction = "The temporary TypeScript canary is dependency-free; existing Python package boundaries remain unchanged."
reviewed_paths = ["src/mixed_canary.ts"]

[incremental_refactor]
target = "Prove the fixed Python-plus-TypeScript profile on a protected pull request."
completed_step = "Added one dependency-free TypeScript production function."

[review_handoff]
summary = "DERIVED_FROM_AUTHENTICATED_EVIDENCE"
remaining_risks = ["DERIVED_FROM_AUTHENTICATED_EVIDENCE"]

[human_review]
naming = "mixedStackLabel names the single canary responsibility."
cohesion = "The temporary module owns only the TypeScript canary label."
intended_behavior = "Both fixed language profiles run in the same protected evaluation."
reviewability = "One production function, one direct test, and one characterization scenario."

[[module_boundaries]]
path = "src/mixed_canary.ts"
owner_path = "src/supportability_gate/__init__.py"
basis = "responsibility"
justification = "The temporary module owns only one TypeScript runtime label; the package initializer remains the existing Python package boundary."
