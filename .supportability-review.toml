schema_version = "1.0"

[behavior]
intended_behavior = "The protected Gate blocks one deliberately over-complex TypeScript function while retaining Python coverage."
proof = "src/mixed_canary.ts exceeds complexity 10 while tests/mixed_canary.test.ts preserves runtime behavior."

[characterization]
captured_behavior = "One stable TypeScript scenario runs beside the retained Python scenarios."
proof = "tests/characterization/s13-mixed-stack-canary.characterization.mjs"

[separation_of_concerns]
before = "The passing canary proved both fixed profiles execute."
after = "The same temporary TypeScript function deliberately exceeds only the fixed complexity threshold."

[[separation_of_concerns.boundaries]]
path = "src/mixed_canary.ts"
kind = "function"
symbol = "mixedStackLabel"
before = "The temporary function passed at complexity one."
after = "The temporary function preserves its label while exceeding complexity ten."

[architecture]
dependency_direction = "The TypeScript canary is dependency-free; existing Python package boundaries remain unchanged."
reviewed_paths = ["src/mixed_canary.ts"]

[responsibility_boundary]
path = "src/mixed_canary.ts"
owns = "One temporary TypeScript runtime label used for protected blocking proof."
does_not_own = "Gate policy, Python package behavior, or product functionality."

[incremental_refactor]
target = "Prove the fixed Python-plus-TypeScript profile blocks excessive TypeScript complexity."
completed_step = "Raised only the temporary TypeScript function above the fixed threshold."

[review_handoff]
summary = "DERIVED_FROM_AUTHENTICATED_EVIDENCE"
remaining_risks = ["DERIVED_FROM_AUTHENTICATED_EVIDENCE"]

[human_review]
naming = "mixedStackLabel still names the single canary responsibility."
cohesion = "The temporary module owns only the deliberate TypeScript complexity block."
intended_behavior = "Both fixed profiles run and Gate 1 blocks the TypeScript threshold violation."
reviewability = "One deliberately over-complex function isolates the expected block."

[[module_boundaries]]
path = "src/mixed_canary.ts"
owner_path = "src/mixed_canary.ts"
basis = "responsibility"
justification = "The dependency-free temporary module is self-owned and exists only for the blocking canary."
