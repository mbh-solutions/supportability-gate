schema_version = "1.0"
module_boundaries = []

[behavior]
intended_behavior = "The private canonical repository preserves the exact S03 webhook delivery metadata that previously existed only in the intentionally deleted proof repository."
proof = "docs/s03_webhook_deliveries.jsonl"

[characterization]
captured_behavior = "The preserved JSONL contains 29 delivery records, 8,271 bytes, SHA-256 1114965c02a2e91587988fdb2a8fac92c005214f1954fdd7968c20f8d8d45e9a, and no raw webhook bodies or secrets."
proof = "docs/s03_webhook_deliveries.jsonl; docs/product_completion_contract.md"

[separation_of_concerns]
before = "The only accessible raw copy of the S03 delivery metadata remained inside a deleted proof checkout in the Recycle Bin."
after = "The canonical private repository owns the immutable evidence file; runtime code, workflows, policy, and the historical record remain unchanged."
boundaries = []

[architecture]
dependency_direction = "The archived evidence is inert data under docs and has no executable dependency on or from the Supportability Gate runtime."
reviewed_paths = [".supportability-review.toml", "docs/s03_webhook_deliveries.jsonl"]

[responsibility_boundary]
path = "docs/s03_webhook_deliveries.jsonl"
owns = "The exact S03 webhook delivery metadata recovered from the intentionally deleted proof repository."
does_not_own = "Runtime behavior, workflow execution, policy, secrets, raw webhook bodies, or live webhook handling."

[incremental_refactor]
target = "Preserve the one unique S03 evidence artifact without changing executable behavior."
completed_step = "Copied the exact verified JSONL bytes into the canonical private repository and bound this review evidence to the archival change."

[review_handoff]
summary = "DERIVED_FROM_AUTHENTICATED_EVIDENCE"
remaining_risks = ["DERIVED_FROM_AUTHENTICATED_EVIDENCE"]

[human_review]
naming = "The destination name identifies the S03 webhook delivery evidence directly."
cohesion = "The JSONL remains one immutable evidence artifact under docs; no runtime responsibility was added."
intended_behavior = "Merging the change makes the unique raw S03 metadata durable before its recycled proof checkout is permanently removed."
reviewability = "The product diff is one exact data file; this TOML records its scope for deterministic gate review."
