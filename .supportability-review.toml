schema_version = "1.0"
module_boundaries = []

[behavior]
intended_behavior = "Characterization verification uses the head contract only for the exact fixed-profile expansion."
proof = "Exact mixed-canary captures no longer produce unauthenticated characterization evidence."

[characterization]
captured_behavior = "Hosted capture selected the exact expansion contract while verification remained on the base Python contract."
proof = "Run 33294265794 characterization-result.json and exact capture artifacts."

[separation_of_concerns]
before = "The transition predicate lived in gate policy and characterization always verified with the base contract."
after = "Contract owns the transition identity; gate policy re-exports it; characterization applies it before capture verification."

[[separation_of_concerns.boundaries]]
path = "src/supportability_gate/contract.py"
kind = "function"
symbol = "is_profile_expansion"
before = "The transition predicate was owned by outward gate policy."
after = "The inward contract module owns the exact fixed-profile transition identity."

[[separation_of_concerns.boundaries]]
path = "src/supportability_gate/contract.py"
kind = "module"
symbol = "src/supportability_gate/contract.py"
before = "Fixed adapter identities were owned by outward gate policy."
after = "The inward contract module owns the fixed adapter identities used to define transitions."

[[separation_of_concerns.boundaries]]
path = "src/supportability_gate/gate_policy.py"
kind = "module"
symbol = "src/supportability_gate/gate_policy.py"
before = "Gate policy implemented the transition predicate and its adapter identities."
after = "Gate policy re-exports the contract predicate and fixed adapter identities."

[[separation_of_concerns.boundaries]]
path = "src/supportability_gate/gate_policy.py"
kind = "function"
symbol = "is_profile_expansion"
before = "Gate policy implemented the transition rule directly."
after = "Gate policy preserves the existing callable while delegating to the inward contract owner."

[[separation_of_concerns.boundaries]]
path = "src/supportability_gate/characterization.py"
kind = "function"
symbol = "verify_evidence"
before = "The verifier parsed only the base contract."
after = "The verifier selects the candidate only when the contract predicate passes."

[architecture]
dependency_direction = "Contract remains inward; gate policy and characterization depend on it without a new outward edge."
reviewed_paths = ["src/supportability_gate/contract.py", "src/supportability_gate/gate_policy.py", "src/supportability_gate/characterization.py"]

[responsibility_boundary]
path = "src/supportability_gate/contract.py"
owns = "Canonical contract identity and the exact fixed-profile transition."
does_not_own = "Quality execution, characterization capture, or merge qualification."

[incremental_refactor]
target = "Align characterization verification with the protected fixed-profile transition."
completed_step = "Moved one predicate inward and selected the candidate only through it."

[review_handoff]
summary = "DERIVED_FROM_AUTHENTICATED_EVIDENCE"
remaining_risks = ["DERIVED_FROM_AUTHENTICATED_EVIDENCE"]

[human_review]
naming = "is_profile_expansion retains the existing exact transition name."
cohesion = "Contract identity, gate enforcement, and characterization verification remain separate."
intended_behavior = "Arbitrary contract changes retain base-contract verification and blocking."
reviewability = "One predicate moved inward and one verifier selection added."
